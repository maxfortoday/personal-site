import type { StatRow } from '../types';

const data: Record<string, StatRow> = {
  age: {
    label: 'Current age',
    value: 0,
  },
  location: {
    label: 'Current city',
    value: 'Givatayim',
  },
};

export default data;
